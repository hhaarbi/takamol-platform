# تقرير تقني شامل — منصة تكامل (Msark)
**تاريخ التقرير:** مايو 2026  
**المُعِد:** مراجعة تقنية آلية  
**الجهة:** شركة هاشم عبد الحكيم للاستشارات الهندسية

---

## 1. نظرة عامة

منصة **مسارك (Msark)** هي نظام إدارة داخلي متكامل مبني لشركة هاشم عبد الحكيم للاستشارات الهندسية، تهدف إلى تنظيم وإدارة المشاريع والمهام والموارد البشرية والعملاء داخل المنظومة الواحدة. تعتمد المنصة على واجهة عربية كاملة مع دعم RTL، وتستهدف الاستخدام الداخلي من قِبل الإدارة والموظفين والمستقلين.

---

## 2. التقنيات المستخدمة (Tech Stack)

### 2.1 الواجهة الأمامية (Frontend)
| التقنية | الإصدار | الغرض |
|---------|---------|-------|
| React | 18 | بناء واجهة المستخدم |
| TypeScript | — | سلامة الأنواع end-to-end |
| Vite | — | أداة البناء والتطوير |
| Tailwind CSS | — | نظام التصميم |
| shadcn/ui | — | مكتبة مكونات الواجهة (30+ مكوّن) |
| Wouter | 3.7.1 | نظام التوجيه (Routing) |
| TanStack React Query | — | إدارة حالة البيانات والـ caching |
| Recharts | — | الرسوم البيانية والإحصائيات |
| Sonner | — | إشعارات toast |
| Lucide React | — | مكتبة الأيقونات |
| SuperJSON | — | تسلسل البيانات (Dates, BigInt) |

### 2.2 الخادم (Backend)
| التقنية | الإصدار | الغرض |
|---------|---------|-------|
| Node.js | — | بيئة التشغيل |
| Express.js | 5.2 | إطار الخادم |
| tRPC | — | طبقة الـ API متكاملة النوع |
| Drizzle ORM | — | التعامل مع قاعدة البيانات |
| bcryptjs | — | تشفير كلمات المرور |
| jose (JWT) | — | جلسات المستخدمين |
| dotenv | — | متغيرات البيئة |

### 2.3 قاعدة البيانات
| التقنية | الغرض |
|---------|-------|
| MySQL | قاعدة البيانات الرئيسية |
| Drizzle Migrations | إدارة التغييرات على الـ Schema |

### 2.4 البنية التحتية
| العنصر | التفاصيل |
|--------|---------|
| خادم الاستضافة | Contabo |
| رفع الملفات | Forge API (proxy خارجي) |
| منفذ التشغيل | Auto-detect من 3000 فما فوق |
| الاختبارات | Vitest |

---

## 3. هيكل المشروع

```
takamol-platform/
├── client/              ← الواجهة الأمامية (React)
│   └── src/
│       ├── pages/       ← 16 صفحة
│       ├── components/  ← 61 مكوّن
│       ├── contexts/    ← AuthContext, ThemeContext
│       ├── hooks/       ← useMobile
│       └── lib/         ← إعداد tRPC
│
├── server/              ← الخادم (Node.js)
│   ├── _core/           ← النواة (Express, tRPC, Auth, Env)
│   ├── routers.ts       ← جميع الـ API endpoints (642 سطر)
│   ├── db.ts            ← طبقة قاعدة البيانات (578 سطر)
│   ├── internalAuth.ts  ← نظام الجلسات (JWT)
│   └── storage.ts       ← رفع الملفات
│
├── drizzle/             ← قاعدة البيانات
│   ├── schema.ts        ← تعريف 14 جدول (312 سطر)
│   └── *.sql            ← ملفات الـ migrations
│
├── shared/              ← كود مشترك
│   ├── types.ts
│   └── const.ts
│
└── src/                 ← ⚠️ كود قديم (غير مستخدم)
    ├── app.js
    └── controllers/     ← 7 controllers قديمة (Sequelize + PostgreSQL)
```

**إجمالي حجم الكود:**
- ملفات TypeScript في الخادم: **23 ملف**
- ملفات TSX في الواجهة: **16 صفحة + 61 مكوّن**
- إجمالي أسطر الملفات الرئيسية: **1,652 سطر**

---

## 4. قاعدة البيانات

### 4.1 الجداول (14 جدول)

| الجدول | الغرض | الحقول الرئيسية |
|--------|-------|----------------|
| `users` | مستخدمو OAuth | openId, name, email, role, lastSignedIn |
| `internal_accounts` | حسابات يوزرنيم/باسورد | username, passwordHash, role, employeeId, isActive |
| `departments` | الأقسام الإدارية | nameAr, nameEn, managerId |
| `employees` | الموظفون | fullName, email, departmentId, salary, skills, performanceScore |
| `freelancers` | المستقلون | fullName, email, specialty, hourlyRate, rating, inviteStatus |
| `clients` | العملاء | companyName, contactName, email, industry |
| `contracts` | العقود | clientId, title, value, status, startDate, endDate |
| `projects` | المشاريع | title, clientId, serviceType, status, priority, budget, progress |
| `project_members` | فريق المشروع | projectId, memberType, employeeId/freelancerId |
| `tasks` | المهام | projectId, title, status, priority, assigneeType, dueDate |
| `task_comments` | تعليقات المهام | taskId, authorType, authorId, content |
| `task_attachments` | مرفقات المهام | taskId, fileName, fileUrl, mimeType |
| `notifications` | الإشعارات | recipientId, type, title, isRead |
| `activity_log` | سجل الأنشطة | actorId, action, entityType, entityTitle |

### 4.2 القيم المحددة مسبقاً (Enums)

**أدوار المستخدمين:**
`admin` | `manager` | `employee` | `freelancer`

**حالات المشروع:**
`draft` | `active` | `on_hold` | `under_review` | `completed` | `cancelled`

**حالات المهمة:**
`pending` | `in_progress` | `under_review` | `completed` | `cancelled`

**مستويات الأولوية:**
`low` | `medium` | `high` | `urgent`

**أنواع الخدمات (7 أنواع):**
`brand_identity` | `visual_production` | `advertising_campaigns` | `events` | `influencer_marketing` | `digital_presence` | `website_design`

### 4.3 ملفات الـ Migrations
| الملف | المحتوى |
|-------|---------|
| `0000_purple_jackal.sql` | إنشاء الجداول الأساسية |
| `0001_nasty_impossible_man.sql` | تحديثات على الـ Schema |
| `0002_big_nextwave.sql` | تحديثات إضافية |

---

## 5. نظام المصادقة (Authentication)

تعتمد المنصة على نظامين متوازيين:

### 5.1 المصادقة الداخلية (Internal Auth)
```
المستخدم → يوزرنيم + باسورد
    ↓
bcrypt.compare() → التحقق من الباسورد المشفر
    ↓
JWT (HS256) → صالح 7 أيام
    ↓
Cookie: "msark_session" → يُخزن في المتصفح
    ↓
في كل طلب → jwtVerify() للتحقق
```

### 5.2 مصادقة OAuth (Manus Platform)
- مسار الـ callback: `/api/oauth/callback`
- Cookie: `app_session_id` — صالحة سنة
- تُستخدم لمستخدمي النظام الخارجي

### 5.3 مستويات الحماية في الـ API
| النوع | الوصف |
|-------|-------|
| `publicProcedure` | متاح بدون تسجيل دخول |
| `protectedProcedure` | يتطلب تسجيل دخول |
| `adminProcedure` | يتطلب دور Admin |

---

## 6. الصفحات والميزات

### 6.1 قائمة الصفحات المطبقة

| الصفحة | المسار | الوظيفة |
|--------|--------|---------|
| تسجيل الدخول | `/login` | نموذج يوزرنيم/باسورد |
| لوحة التحكم | `/dashboard` | KPIs + رسوم بيانية + آخر الأنشطة |
| المشاريع | `/projects` | قائمة + فلترة + إنشاء |
| تفاصيل مشروع | `/projects/:id` | الفريق + الميزانية + المهام |
| المهام | `/tasks` | قائمة + فلترة + إنشاء |
| تفاصيل مهمة | `/tasks/:id` | تعليقات + مرفقات |
| الموظفون | `/employees` | إدارة كاملة (CRUD) |
| المستقلون | `/freelancers` | إدارة كاملة + تقييم |
| العملاء | `/clients` | إدارة بيانات العملاء |
| العقود | `/contracts` | عقود مرتبطة بالعملاء |
| التقارير | `/reports` | إحصائيات وتحليلات |
| الحسابات | `/accounts` | إدارة حسابات (Admin فقط) |
| الإشعارات | `/notifications` | مركز الإشعارات |

### 6.2 لوحة التحكم — الإحصائيات
تُشغّل **11 استعلاماً متوازياً** في قاعدة البيانات:
- إجمالي/نشط/مكتمل من المشاريع
- إجمالي/معلق/قيد التنفيذ/مكتمل من المهام
- عدد الموظفين النشطين
- عدد المستقلين النشطين
- عدد العملاء النشطين
- إجمالي قيمة العقود النشطة (الإيرادات)

---

## 7. الـ API — نقاط النهاية

**المسار الأساسي:** `/api/trpc/`

| Router | العمليات |
|--------|---------|
| `auth` | me, logout |
| `internalAuth` | login, me, logout, createAccount, listAccounts, updateAccount, deleteAccount |
| `dashboard` | stats, projectsByServiceType, projectsByStatus, recentActivity |
| `departments` | list, create, update |
| `employees` | list, getById, create, update, delete |
| `freelancers` | list, getById, create, update |
| `clients` | list, getById, create, update, delete |
| `contracts` | list, create, update |
| `projects` | list, getById, create, update, delete, getMembers, addMember, removeMember |
| `tasks` | list, getById, create, update, delete, getComments, addComment, getAttachments, addAttachment |
| `notifications` | list, unreadCount, markRead, markAllRead |

**إجمالي العمليات المتاحة: 38 endpoint**

---

## 8. تقييم جودة الكود

### 8.1 نقاط القوة ✅

| الجانب | التقييم |
|--------|--------|
| Type Safety | ممتاز — TypeScript كامل من الـ DB حتى الـ UI |
| هيكل الكود | منطقي ومنظم — فصل واضح بين المسؤوليات |
| دعم العربية/RTL | احترافي — مبني من الأساس |
| مكتبة المكونات | shadcn/ui — جاهزة وقابلة للتخصيص |
| إدارة قاعدة البيانات | Drizzle ORM — حديث وآمن |
| التوثيق الذاتي | Zod schemas على جميع المدخلات |
| سجل الأنشطة | موجود — جيد للمراجعة والتدقيق |
| Soft Delete | يُعطّل بدلاً من حذف — جيد لحفظ البيانات |
| الإشعارات التلقائية | تُنشأ عند تعيين المهام |

### 8.2 المشاكل الحرجة 🔴

#### المشكلة الأولى: غياب الحماية على الـ API
```
الوضع الحالي: 95% من الـ endpoints تستخدم publicProcedure
الخطر: أي شخص يعرف عنوان الـ API يقدر يقرأ/يعدل/يحذف البيانات
```

| Endpoint | الحماية الحالية | يجب أن تكون |
|----------|----------------|-------------|
| employees.list | public | protected |
| employees.create | public | manager/admin |
| employees.delete | public | admin |
| projects.delete | public | manager/admin |
| clients.create | public | protected |
| contracts.create | public | manager/admin |

#### المشكلة الثانية: JWT Secret الافتراضي
```
إذا كان JWT_SECRET فارغاً في .env، يستخدم النظام:
'msark-internal-secret-2024'  ← مفتاح معروف للجميع
```
يعني أي شخص يقدر يصنع جلسة مزوّرة.

#### المشكلة الثالثة: actorId ثابت في سجل الأنشطة
```
actorId: 1  ← دائماً، بغض النظر عن المستخدم الفعلي
actorName: 'النظام'
```
سجل الأنشطة لا يعكس الشخص الحقيقي الذي نفّذ الإجراء.

### 8.3 المشاكل المتوسطة 🟡

| المشكلة | التأثير | الحل |
|---------|---------|-----|
| لا Pagination على القوائم | أداء سيء مع البيانات الكبيرة | إضافة limit/offset |
| لا Database Indexes | بطء الاستعلامات مع النمو | إضافة indexes على الحقول المستخدمة في البحث |
| كود قديم في `/src/` | تشويش وخطر تعارض | حذف أو نقل كملف أرشيف |
| لا Rate Limiting | قابلية لهجمات brute force | إضافة express-rate-limit |
| لا CSRF Protection | ثغرة أمنية | إضافة csrf middleware |
| رفع الملفات بلا قيود | قبول أي نوع ملف | التحقق من نوع ومساحة الملف |
| الإشعارات بـ Polling | زيادة الطلبات (كل 30 ثانية) | الانتقال لـ WebSocket أو SSE |

---

## 9. الميزات المطبقة مقابل المخطط لها

| الميزة | الحالة |
|--------|--------|
| إدارة المشاريع الكاملة | ✅ مطبق |
| إدارة المهام مع تعليقات ومرفقات | ✅ مطبق |
| إدارة الموظفين والأقسام | ✅ مطبق |
| إدارة المستقلين | ✅ مطبق |
| إدارة العملاء والعقود | ✅ مطبق |
| لوحة تحكم بإحصائيات | ✅ مطبق |
| نظام إشعارات داخلي | ✅ مطبق (بدون real-time) |
| سجل أنشطة | ✅ مطبق (ناقص actorId) |
| تعدد الأدوار | ✅ مطبق في الـ Schema |
| صفحة التقارير | ⚠️ هيكل موجود، المحتوى ناقص |
| دعوة المستقلين بالإيميل | ❌ حقل موجود في DB فقط |
| إشعارات الإيميل | ❌ nodemailer مثبت وغير مفعّل |
| إشعارات real-time | ❌ غير موجود |
| نظام المواعيد | ❌ موجود في README فقط |
| نظام عروض الأسعار | ❌ موجود في README فقط |
| تصدير التقارير | ❌ غير موجود |
| اختبارات شاملة | ⚠️ بنية موجودة، تغطية محدودة |

---

## 10. متغيرات البيئة المطلوبة

```bash
# قاعدة البيانات
DATABASE_URL=mysql://user:password@host:3306/database

# الأمان
JWT_SECRET=<مفتاح عشوائي طويل وفريد>

# التطبيق
VITE_APP_ID=<معرف التطبيق>
NODE_ENV=production

# OAuth (اختياري)
OAUTH_SERVER_URL=<رابط سيرفر OAuth>
OWNER_OPEN_ID=<معرف المالك>

# تخزين الملفات
BUILT_IN_FORGE_API_URL=<رابط API الملفات>
BUILT_IN_FORGE_API_KEY=<مفتاح API الملفات>
```

> **تحذير:** ملف `.env.example` يحتوي على قيم افتراضية للـ `.env.example` وليس للإنتاج. يجب استبدال جميع القيم بقيم حقيقية وسرية.

---

## 11. التقييم العام

```
┌─────────────────────────────────────────────────────────┐
│                   التقييم الإجمالي                       │
├────────────────────┬────────────────────────────────────┤
│ جودة الكود         │ ████████░░  80/100                  │
│ الأمان             │ █████░░░░░  50/100  ← الأولوية الأولى│
│ اكتمال الميزات     │ ███████░░░  70/100                  │
│ الأداء والتوسع     │ ██████░░░░  60/100                  │
│ تجربة المستخدم     │ ████████░░  80/100                  │
│ التوثيق            │ ████░░░░░░  40/100                  │
├────────────────────┴────────────────────────────────────┤
│ التقييم الكلي:  ████████░░░  65/100                      │
└─────────────────────────────────────────────────────────┘
```

---

## 12. التوصيات والأولويات

### الأولوية الأولى — فورية (أمان)
1. **تحويل جميع الـ endpoints** من `publicProcedure` إلى `protectedProcedure` أو `adminProcedure` حسب الدور
2. **التحقق من `JWT_SECRET`** في `.env` وإضافة قيمة قوية وعشوائية
3. **إصلاح `actorId`** في سجل الأنشطة ليعكس المستخدم الفعلي

### الأولوية الثانية — قريبة (استقرار)
4. **إضافة Pagination** على جميع قوائم البيانات
5. **إضافة Database Indexes** على الحقول المستخدمة في البحث والفلترة
6. **حذف الكود القديم** في مجلد `/src/`
7. **إضافة Rate Limiting** على نقاط الـ API

### الأولوية الثالثة — مستقبلية (تطوير)
8. **إشعارات Real-time** عبر WebSocket أو Server-Sent Events
9. **إشعارات الإيميل** عبر تفعيل Nodemailer
10. **صفحة التقارير** بتفاصيل كاملة
11. **تصدير البيانات** PDF / Excel
12. **نظام دعوة المستقلين** عبر الإيميل
13. **تغطية اختبارات** أوسع

---

## 13. خلاصة

منصة مسارك مبنية على **أساس تقني قوي وحديث** يعكس خبرة جيدة في اختيار التقنيات وتنظيم الكود. الواجهة الأمامية احترافية وتدعم العربية بشكل كامل، وبنية الـ API منطقية ومنظمة.

**أكبر تحدي حالي** هو الجانب الأمني؛ إذ إن غياب الحماية على معظم نقاط الـ API يجعل النظام عرضة للوصول غير المصرح به. هذا الأمر يجب معالجته **قبل الاستخدام الفعلي في الإنتاج**.

بعد معالجة المشاكل الأمنية، تصبح المنصة جاهزة للاستخدام الداخلي مع خارطة طريق واضحة لإضافة الميزات المتبقية.

---
*تم إعداد هذا التقرير بناءً على مراجعة كاملة لكود المصدر في مايو 2026*
